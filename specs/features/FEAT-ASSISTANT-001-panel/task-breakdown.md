# Task Breakdown

1. Create shell-level assistant panel state and layout ownership in the authenticated app shell.
2. Add the topbar trigger and open/close behavior.
3. Build the docked left-side assistant panel container with theme-safe styling.
4. Add horizontal resize behavior with constrained width.
5. Adapt or wrap the existing chat feature so it renders correctly inside the panel.
6. Replace the current plain composer treatment with the rounded integrated composer surface.
7. Add route-persistence/session-persistence behavior for panel state if accepted in implementation.
8. Add frontend tests for trigger, panel behavior, resize behavior, and composer UX.
9. Update validation evidence after implementation.
