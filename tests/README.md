# Tests

This folder is reserved for cross-cutting test suites that support spec-driven validation across the repository.

Suggested structure as the repository grows:

- `contract/`
- `integration/`
- `e2e/`

Each suite should reference the feature and acceptance criteria identifiers it validates.
