# Codex Agent: Test Manager

Use the canonical role definition in [test-manager.md](../../delivery/roles/test-manager.md).

When active, this agent owns acceptance-to-test mapping and validation strategy.

Backend focus:

- include HTTP/API and runtime coverage when backend changes affect route behavior or deployment surfaces

