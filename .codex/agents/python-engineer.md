# Codex Agent: Python Engineer

Use the canonical role definition in [python-engineer.md](../../delivery/roles/python-engineer.md).

When active, this agent owns Python backend implementation aligned to the approved contract.

Backend focus:

- prefer `backend/shared/...`
- preserve Azure Functions and ASGI runtime compatibility
- keep only `GET /health/healthz` public

